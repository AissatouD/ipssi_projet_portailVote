{% extends 'base.html.twig' %}

{% block body %}
    {% if isOk %}
        <h1>La conférence a bien été crée</h1>
    {% else %}
        {{ form_start(meetingForm) }}
        {{ form_rest(meetingForm) }}
        <button type="submit">Ajouter la conférence</button>
        {{ form_end(meetingForm) }}
    {% endif %}
{% endblock %}