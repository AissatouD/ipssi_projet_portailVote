{% extends 'base.html.twig' %}

{% block body %}
    <h2>Top 5 Conférence</h2>
   <table>
        <thead>
        <tr>
            <th>Nom</th>
            <th>Description</th>
            <th>Date</th>
            <th>Lieu</th>
            <th>Note</th>
            
        </tr>
        </thead>
        <tbody>
        {% for meeting in topMeeting %}
        <tr>
            <td>{{ meeting.title }}</td>
            <td>{{ meeting.description }}</td>
            <td>{{ meeting.date|date("m/d/Y") }}</td>
            <td>{{ meeting.location}}</td>
            <td>{{ meeting.note}}</td>
            <td><a href="{{ path('voteapp_vote_add', {id: meeting.id}) }}">Noter</a></td>
            <td><a href="{{ path('meetingapp_meeting_edit', {id: meeting.id}) }}">Edit</a></td>
            <td><a href="{{ path('meetingapp_meeting_delete', {id: meeting.id}) }}">Delete</a></td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}